name: dot
help: Run scripts on dot files
version: 0.1.0

commands:
    - name: waybar
      catch_all:
          label: params
          help: parameters
          catch_help: true

    - name: tmux
      catch_all:
          label: params
          help: parameters
          catch_help: true

    - name: hypr
      catch_all:
          label: params
          help: parameters
          catch_help: true
