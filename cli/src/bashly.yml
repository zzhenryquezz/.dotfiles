name: dot
help: Run scripts on dot files
version: 0.1.0

commands:
    - name: waybar
      commands:
          - name: reload
            help: reload waybar

          - name: watch
            help: watch for changes
    - name: tmux
      commands:
          - name: project
            help: open project

          - name: create
            help: create a tmux session
